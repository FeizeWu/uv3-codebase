# 训练速度测试

## 3B 模型处理 1B 图文样本的时间估算

测试条件：

- 模型参数量：3.13B
- 每台机器：8 张 GPU
- 单卡 batch size：12
- 单机全局 batch size：96
- 输入包含真实文本，text encoder 为冻结的 Qwen3.5-9B
- 文本固定 pad 到 1024 tokens，图片 token 固定为 256
- 使用 FSDP2、Flex Attention 和静态 `torch.compile`
- MMDiT 与 text encoder 均启用 `torch.compile`

单机实测稳态结果：

> 本节初始结果使用 trainer 的 `timing` 计算段口径：batch 已经取出后开始计时，不包含下一批数据读取和 CPU 分桶/tokenize。后面的长度桶章节同时给出严格 wall-clock 口径。

| 配置 | 耗时/step | 吞吐 | 峰值显存/卡 |
|---|---:|---:|---:|
| AdamW + 关闭 self-flow | 3.16 秒 | 30.4 样本/秒 | 64.8 GiB |
| Muon + self-flow | 4.54 秒 | 21.1 样本/秒 | 66.4 GiB |

AdamW + 关闭 self-flow 相比 Muon + self-flow 综合提速约 **1.44 倍**，耗时下降约 **30.4%**。

以下按 1B 图文样本计算。多机按接近线性扩展估算，扩展效率依次采用 100%、99%、98%、97%、96%、95%；没有计入 FSDP 显存进一步下降后增大 batch 可能带来的额外利用率收益。

| 机器数 | GPU 数 | AdamW + 关闭 self-flow | Muon + self-flow |
|---:|---:|---:|---:|
| 1 | 8 | **381.1 天** | 547.3 天 |
| 2 | 16 | **192.5 天** | 276.4 天 |
| 4 | 32 | **97.2 天** | 139.6 天 |
| 8 | 64 | **49.1 天** | 70.5 天 |
| 16 | 128 | **24.8 天** | 35.6 天 |
| 32 | 256 | **12.5 天** | 18.0 天 |

理想完全线性扩展时，32 台机器分别约需 **11.9 天**和 **17.1 天**。按上述扩展假设，AdamW + 关闭 self-flow 在 32 台机器上的预计时间约为 **11.9～12.5 天**。

## Qwen 静态文本长度桶

在 AdamW、关闭 self-flow、单卡 batch size 12 的 3B 配置上，对 Qwen 使用五个静态长度桶：

- 桶长度：`512 / 640 / 768 / 896 / 1024`
- 调度权重：`5 / 4 / 4 / 3 / 4`，匹配当前数据的文本长度分布
- 每个 rank 在同一步使用相同长度桶
- 每张卡保留一份 Qwen 权重，并缓存五张静态 `torch.compile` 图
- 所有图均为 `dynamic=False`
- Qwen 输出补回 1024，MMDiT 继续使用单一 1024-token 静态图
- 前 20 步用于五张图的编译预热，使用后续两个完整 10 步窗口计时

严格 A/B 的训练计算段结果（不含下一批读取和 CPU 分桶/tokenize）：

| Qwen 输入方案 | 耗时/step | 单机吞吐（全局 BS=96） | 峰值显存/卡 |
|---|---:|---:|---:|
| 固定 1024 baseline | 3.157 秒 | 30.41 样本/秒 | 64.8 GiB |
| 四桶：640～1024 | 2.755 秒 | 34.84 样本/秒 | 64.8 GiB |
| 五桶：512～1024 | **2.729 秒** | **35.17 样本/秒** | 64.8 GiB |

五桶相比固定 1024 的训练计算段：

- 吞吐提升 **15.68%**
- 单步时间下降 **13.55%**
- 加速比 **1.157 倍**
- 五桶相比四桶额外提升约 **0.94%**

严格 wall-clock 结果使用预热后最后 10 步的累计 `spd` 差分，包含数据读取、CPU 分桶和 tokenize：

| Qwen 输入方案 | wall-clock 耗时/step | 单机端到端吞吐（全局 BS=96） |
|---|---:|---:|
| 固定 1024 baseline | 3.293 秒 | 29.15 样本/秒 |
| 五桶：512～1024 | **2.923 秒** | **32.84 样本/秒** |

五桶的真实端到端吞吐提升为 **12.65%**，wall-clock 单步时间下降 **11.23%**。计算段与 wall-clock 收益之间约 3 个百分点的差距，主要来自当前分桶路径会在 CPU 上额外做一次长度 tokenize，随后训练路径再次 tokenize。

按 1B 图文样本估算：

| 机器数 | GPU 数 | 固定 1024 | Qwen 五桶 |
|---:|---:|---:|---:|
| 1 | 8 | 397.0 天 | **352.4 天** |
| 2 | 16 | 200.5 天 | **178.0 天** |
| 4 | 32 | 101.3 天 | **89.9 天** |
| 8 | 64 | 51.2 天 | **45.4 天** |
| 16 | 128 | 25.8 天 | **22.9 天** |
| 32 | 256 | 13.1 天 | **11.6 天** |

多机时间继续采用 100%、99%、98%、97%、96%、95% 的扩展效率假设。按严格 wall-clock 口径，五桶方案在单机处理 1B 样本时预计节省约 **44.6 天**。

## Tokenize once、FSDP 不 reshard 与 VAE compile 实测

在五桶方案上继续逐项叠加优化。每项均使用相同真实图文数据、全局 batch size 96、8 张 H20；前 20 step 预热，使用 step 30 到 step 40 的累计 `spd` 差分计算严格 wall-clock。结果如下：

| 逐项配置 | wall-clock 耗时/step | 单机吞吐 | 相对上一项吞吐 | 相对五桶 baseline | 训练计算段/step | 峰值显存/卡 |
|---|---:|---:|---:|---:|---:|---:|
| 五桶 baseline | 2.923 秒 | 32.84 样本/秒 | — | — | 2.729 秒 | 64.82 GiB |
| + tokenize once、pinned token batch | 2.902 秒 | 33.08 样本/秒 | **+0.73%** | +0.73% | 2.729 秒 | 64.82 GiB |
| + `reshard_after_forward=False` | 2.874 秒 | 33.40 样本/秒 | **+0.97%** | +1.71% | 2.694 秒 | 67.14 GiB |
| + VAE `_encode` `max-autotune` compile | **2.852 秒** | **33.67 样本/秒** | **+0.79%** | **+2.51%** | 2.671 秒 | 66.87 GiB |
| + fused AdamW（不推荐） | 2.859 秒 | 33.57 样本/秒 | -0.27% | +2.24% | **2.657 秒** | 66.81 GiB |
| + MMDiT 同步五桶（新推荐） | **2.428 秒** | **39.55 样本/秒** | **+17.47%**¹ | **+20.42%** | **2.321 秒** | 67.03 GiB |
| 上一行改为 BS14（不推荐） | 2.838 秒 | 39.47 样本/秒 | **-0.19%** | +20.18% | 2.666 秒 | 73.37 GiB |
| 上一行改为 BS16（不推荐） | 3.225 秒 | 39.69 样本/秒 | **+0.36%** | +20.86% | 3.017 秒 | 83.06 GiB |
| MMDiT `reduce-overhead`（不推荐） | 6.097 秒 | 15.75 样本/秒 | **-60.18%** | -52.05% | 6.034 秒 | 80.50 GiB |
| MMDiT `max-autotune-no-cudagraphs`（长训练可选） | **2.398 秒** | **40.03 样本/秒** | **+1.22%** | **+21.89%** | **2.233 秒** | **66.64 GiB** |
| 上一行仅前 20 步 grad clip（不推荐） | 2.400 秒 | 39.99 样本/秒 | **-0.09%** | +21.78% | 2.223 秒 | 66.97 GiB |
| + Qwen 中间 24 层 MLP FP8（质量优先候选） | **2.189 秒** | **43.86 样本/秒** | **+9.57%** | **+33.55%** | 2.017 秒 | 63.60 GiB |
| + Qwen 全部 MLP FP8（平衡候选） | **2.112 秒** | **45.46 样本/秒** | **+13.57%** | **+38.43%** | 1.942 秒 | 62.48 GiB |
| + Qwen 全 Linear FP8（速度上限候选） | **2.008 秒** | **47.81 样本/秒** | **+19.43%** | **+45.57%** | 1.827 秒 | 60.59 GiB |

¹ MMDiT 五桶相对“不使用 fused AdamW”的上一推荐组合计算；fused 行只是负结果记录，不进入叠加基线。

实现与结论：

- 分桶判断长度时保存 `input_ids`，训练阶段不再二次 tokenize；按 `(双缓冲 slot, 桶长度)` 复用 pinned `input_ids/attention_mask`。token 在 Qwen 当前 CUDA stream 上有序异步 H2D。把下一批 token 放到 image prefetch stream 会产生跨 stream 争用，实测回退约 5%，因此没有采用。
- `reshard_after_forward=False` 省掉 backward 前的参数 all-gather，端到端提升 0.97%，每卡峰值显存增加约 2.32 GiB；当前 H20 显存充足，建议启用。
- VAE 只编译固定形状、纯 tensor 的 `vae._encode`，使用 `dynamic=False, mode="max-autotune"`。稳态端到端提升 0.79%；首次编译的 VAE 段约 75 秒，之后使用编译缓存。
- fused AdamW 将可见 optimizer 调用段从约 0.363 秒降至 0.181 秒，但等待转移到更早的同步点，严格 wall-clock 没有收益。保留 `adam_fused` 可选开关，但推荐保持关闭。
- 非 fused 配置不会显式传 `fused=False`，而是保留 PyTorch 的 `fused=None/foreach=None` 自动选择。显式传 `fused=False` 会禁用 torch 2.10 的 CUDA 快速默认路径，在本测试中会把 optimizer 段放大到约 1.55 秒/step。
- MMDiT 同步保留 Qwen 的 `512/640/768/896/1024` 五种静态长度，不再把 text embedding 补回 1024。对应总序列为 `768/896/1024/1152/1280`，全部是 128 的倍数，可直接复用 Flex Attention block size。相对上一推荐组合端到端提升 17.47%，是目前最大单项收益。
- Flex 路径已有外部 `text_attn_mask` 时不再在 MMDiT 内重复构造一份未使用的 document BlockMask。
- 将单卡 batch 从 12 增到 16 后，step 时间从 2.428 秒近似按比例增到 3.225 秒，吞吐只从 39.55 增到 39.69 样本/秒（+0.36%），峰值显存却增加 16.03 GiB。该点已经进入算力/显存带宽受限区，不推荐用 24% 的剩余显存换取不足 0.4% 的吞吐；继续测试 BS14 寻找静态 kernel 的局部甜点位，BS18 暂不进入推荐组合。
- BS14 也只有 39.47 样本/秒，比 BS12 低 0.19%，峰值显存增加 6.34 GiB。BS12/14/16 的吞吐差异都在 0.4% 内，说明已经形成平坦的算力/带宽平台，并不存在靠放大 batch 获取的局部甜点位。BS18 预计还会增加约 8～10 GiB 峰值显存并触碰 96GB 上限，且没有吞吐趋势支持，因此按预设判定停止该点，避免无收益的约 20 分钟冷编译。
- 为避免混淆变量，新增独立的 `text_encoder_compile_mode`；未设置时继承原 `compile_mode`，保持旧配置兼容。测试 `reduce-overhead` 时 Qwen 固定为 `default`，只有 MMDiT 改变。结果吞吐从 39.55 降到 15.75 样本/秒（-60.18%），forward/backward 计算段增到约 5.90 秒/step，CUDA Graph 私有池还让峰值显存增加约 13.47 GiB。该模式与当前 FSDP2 参数视图/collective、Flex Attention 五桶组合明显不适配，严格淘汰；32 台、95% 扩展效率会恶化到 24.18 天。完成 step 40 的测量窗口后提前停止剩余 10 步。
- PyTorch 2.10 的普通 `max-autotune` 同时打开 Triton CUDA Graph；鉴于上一项已经证明 CUDA Graph 路径严重回退，本轮改测 `max-autotune-no-cudagraphs`，只隔离内核搜索收益，Qwen 继续固定为 `default`。严格 step 30→40 吞吐为 40.03 样本/秒，相对 BS12 推荐基线提升 1.22%，计算段从 2.321 降到 2.233 秒/step，峰值显存反而低 0.39 GiB。该配置可用于 1B 长训练，但五个 MMDiT 桶在 8 rank 上首次完整搜索约耗时 71 分钟；编译期间出现的 `No valid triton configs` 是候选内核超出寄存器/共享内存资源后被自动忽略，并非训练 OOM。32 台稳态估算为 9.51 天；加上约 1.18 小时冷启动约 9.56 天，仍比默认编译的 9.63 天短约 1.6 小时。
- grad norm 审计使用相同 seed 与配置，逐步读取 pre-clip norm；该读取会强制 GPU 同步，所以审计 run 只用于统计，不能用于测速。统计到的 41 步中仅 step 0 触发阈值 1.0（norm=1.2188）；step 1～10 最大 0.9961，step 11～20 最大 0.5391，step 21～40 最大 0.4082。随后严格测试“前 20 步每步 clip、之后关闭”：可见 optimizer/clip 段从约 0.160 降至 0.075 秒/step，但异步等待转移到了前面的 Qwen 段，wall-clock 为 2.400 秒、39.99 样本/秒，相对完整 clip 的 max-autotune 基线回退 0.09%。step 40 loss 仅相差约 9e-6，但无端到端收益且长期数据尖峰仍有风险，因此淘汰；默认仍保持每步 clip。
- Qwen FP8 使用 torchao 的 tensorwise dynamic-activation/FP8-weight 路径，在 `torch.compile` 前原地量化冻结 Qwen。H20 的单卡 eager 预筛中，全 Linear FP8 将 12×512 延迟从 0.707 降到 0.531 秒；8 卡严格训练进一步达到 47.81 样本/秒，相对 BF16 max-autotune 基线提升 19.43%，32 台稳态估算降到 7.96 天。
- torchao 默认的 `set_inductor_config=True` 会修改进程全局 Inductor 配置，使已有 MMDiT 五桶缓存失效并重新 autotune。首轮确认开始无意义地重编 MMDiT 后中止；正式实现固定使用 `set_inductor_config=False`，保留 FP8 scaled-mm，但不污染 MMDiT 编译哈希。FP8 五桶首次生成约数分钟；共享 kernel 缓存后，中间 24 层方案从启动到完成 50 step 约 4.6 分钟。
- rowwise FP8 在本机 torchao 0.14.0+git 上产生 NaN，直接淘汰。tensorwise 全量 FP8 没有 NaN，但真实 caption 的误差随文本变长累积，因此新增 `all`、`mlp` 和 `mlp_middle` 三种量化范围。`mlp_middle` 默认只量化 Qwen 32 层中的 `[4, 28)`，保留首尾各 4 层和所有 attention/linear-attention 投影为 BF16。
- trainer 的分阶段 timer 使用 `time.time()` 且不逐段 CUDA synchronize，因此 GPU 异步工作会跨到后续阶段：FP8 日志中 Qwen 段未必变短，而 forward/backward 段明显变短。不能据此声称 MMDiT 本身被 FP8 加速；本文所有选择只依据包含全部异步工作的 step 30→40 严格 wall-clock。

### Qwen FP8 真实文本特征误差

从训练 manifest 中各取 12 条真实 caption，覆盖五个静态桶；只在 attention mask 有效 token 上比较 BF16 与 FP8 的最后层特征：

| 桶长度 | 全 Linear FP8 rel-L2 / cosine | 全部 MLP FP8 rel-L2 / cosine | 中间 24 层 MLP FP8 rel-L2 / cosine |
|---:|---:|---:|---:|
| 512 | 8.28% / 0.9964 | 5.92% / 0.9982 | **4.67% / 0.9989** |
| 640 | 8.88% / 0.9959 | 6.39% / 0.9979 | **5.35% / 0.9985** |
| 768 | 10.39% / 0.9945 | 6.98% / 0.9975 | **5.21% / 0.9986** |
| 896 | 11.93% / 0.9927 | 7.31% / 0.9973 | **6.36% / 0.9980** |
| 1024 | 12.14% / 0.9924 | 7.63% / 0.9970 | **6.17% / 0.9981** |

三种 FP8 方案在 50-step 同 seed 训练中的 step 40 loss 分别为 1.406742 / 1.406670 / 1.406674，BF16 为 1.406734，短程没有可见发散。但随机初始化 MMDiT 的短程 loss 不能证明冻结文本特征误差不会影响最终质量：中间 24 层是质量优先候选，全部 MLP 是速度/误差平衡候选，全 Linear 只作为速度上限；正式 1B 训练前仍需用下游指标或更长 loss A/B 放行。

### MMDiT token-block FP8 训练

torchao `Float8Linear` 同时量化 forward 的 input/weight，以及 backward 的 grad-output。直接转换所有 MMDiT Linear 的缩小模型烟测失败：`norm_out.linear` 等时间条件/调制层只处理原始 batch 维，BS=12 不满足 scaled-mm 的 16 对齐约束。正式实现只转换 4 个 double block 和 16 个 single block 内共 80 个 token 级 Linear；文本/图片序列均为 128 的倍数，排除 batch-only 小矩阵后，缩小模型的 Flex Attention + compile + 3 次 backward 全部 finite。

严格 3B/8 卡结果使用 MMDiT `compile_mode=default`；第一行保持 Qwen BF16 来隔离 MMDiT FP8，后两行再叠加 Qwen FP8：

| 配置 | wall-clock/step | 单机吞吐 | 相对 BF16/default | 训练计算段/step | 峰值显存/卡 | step 40 loss |
|---|---:|---:|---:|---:|---:|---:|
| BF16 MMDiT + BF16 Qwen | 2.428 秒 | 39.55 样本/秒 | — | 2.321 秒 | 67.03 GiB | 1.4067 |
| **token-block FP8 MMDiT + BF16 Qwen** | **1.993 秒** | **48.17 样本/秒** | **+21.81%** | 1.910 秒 | 60.90 GiB | 1.406622 |
| + Qwen 中间 24 层 MLP FP8 | **1.778 秒** | **53.98 样本/秒** | **+36.51%** | 1.692 秒 | 57.47 GiB | 1.406590 |
| + Qwen 全 Linear FP8（速度上限） | **1.581 秒** | **60.72 样本/秒** | **+53.55%** | 1.501 秒 | 54.38 GiB | 1.406667 |

MMDiT FP8 首次五桶编译到 step 10 约 20.5 分钟；两边 FP8 图均缓存后，全 FP8 组合从启动到完成 50 step 约 3.4 分钟。当前没有开启 `enable_fsdp_float8_all_gather`，所以收益来自本地 FP8 GEMM，不依赖压缩 FSDP 通信。50-step loss 和梯度均 finite，但这仍不能替代长程收敛验证：MMDiT FP8 直接改变训练梯度，风险高于冻结 Qwen FP8，三种结果都应视为性能候选而非已证明等质量。

按相同多机扩展效率估算：

| 机器数 | GPU 数 | MMDiT FP8 + BF16 Qwen | + 中间 24 层 Qwen FP8 | 全 Qwen + MMDiT FP8 |
|---:|---:|---:|---:|---:|
| 1 | 8 | 240.27 天 | **214.40 天** | **190.60 天** |
| 2 | 16 | 121.35 天 | **108.28 天** | **96.26 天** |
| 4 | 32 | 61.29 天 | **54.69 天** | **48.62 天** |
| 8 | 64 | 30.96 天 | **27.63 天** | **24.56 天** |
| 16 | 128 | 15.64 天 | **13.96 天** | **12.41 天** |
| 32 | 256 | **7.90 天** | **7.05 天** | **6.27 天** |

阶段一组合（尚未让 MMDiT 使用真实桶长）相对原五桶 baseline 的端到端吞吐累计提升 2.51%。加入 MMDiT 五桶后的**新推荐组合**累计吞吐提升 **20.42%**，单步时间下降 **16.96%**。按 1B 图文样本估算：

| 机器数 | GPU 数 | 五桶 baseline | 阶段一组合 | 新推荐组合（BS12） | BS14 实测 | BS16 实测 | MMDiT max-autotune¹ |
|---:|---:|---:|---:|---:|---:|---:|---:|
| 1 | 8 | 352.4 天 | 343.8 天 | **292.7 天** | 293.2 天 | 291.6 天 | **289.15 天** |
| 2 | 16 | 178.0 天 | 173.6 天 | **147.8 天** | 148.1 天 | 147.3 天 | **146.03 天** |
| 4 | 32 | 89.9 天 | 87.7 天 | **74.7 天** | 74.8 天 | 74.4 天 | **73.76 天** |
| 8 | 64 | 45.4 天 | 44.3 天 | **37.7 天** | 37.8 天 | 37.6 天 | **37.26 天** |
| 16 | 128 | 22.9 天 | 22.4 天 | **19.1 天** | 19.1 天 | 19.0 天 | **18.82 天** |
| 32 | 256 | 11.6 天 | 11.3 天 | **9.63 天** | 9.65 天 | 9.59 天 | **9.51 天** |

¹ 表中为稳态训练时间，未计首次约 71 分钟的五桶内核搜索；32 台总 wall-clock 加上该成本约为 9.56 天。

多机时间仍采用 100%、99%、98%、97%、96%、95% 的扩展效率假设。

在 MMDiT `max-autotune-no-cudagraphs` 的 BF16 Qwen 基线上，三档 Qwen FP8 的 1B 样本时间如下：

| 机器数 | GPU 数 | BF16 Qwen | 中间 24 层 MLP FP8 | 全部 MLP FP8 | 全 Linear FP8 |
|---:|---:|---:|---:|---:|---:|
| 1 | 8 | 289.15 天 | **263.90 天** | **254.60 天** | **242.10 天** |
| 2 | 16 | 146.03 天 | **133.28 天** | **128.59 天** | **122.27 天** |
| 4 | 32 | 73.76 天 | **67.32 天** | **64.95 天** | **61.76 天** |
| 8 | 64 | 37.26 天 | **34.01 天** | **32.81 天** | **31.20 天** |
| 16 | 128 | 18.82 天 | **17.18 天** | **16.58 天** | **15.76 天** |
| 32 | 256 | 9.51 天 | **8.68 天** | **8.37 天** | **7.96 天** |

该表为稳态速度，不含首次几分钟的 FP8 五桶编译；相对 8～9 天训练可忽略。三列不是等质量结论，必须结合上一节特征误差选择。

## 32 台低于 10 天：目标与逐项测试计划

阶段一推荐组合在单机为 33.67 样本/秒；按 32 台、95% 扩展效率为 11.3 天。低于 10 天要求单机至少达到 38.08 样本/秒。MMDiT 五桶实测达到 **39.55 样本/秒、9.63 天**，已越过目标线。后续仍按同一真实数据、8×H20、全局 batch、20 step 预热与 step 30→40 wall-clock 差分逐项验证；正收益才进入下一项的叠加基线，负收益也保留记录和原因分析。

| 顺序 | 待测方案 | 预估收益 | 状态 | 进入推荐组合的判定 |
|---:|---|---:|---|---|
| A | Qwen 与 MMDiT 同步五桶，MMDiT 不再补回 1024 | **实测 +17.47%** | **完成，进入基线** | loss 末位约 1e-4 差异；wall-clock 正收益 |
| B | 基于新显存重新扫单卡 BS 12/14/16/18 | BS14 -0.19%；BS16 +0.36% | **完成，不进入基线** | BS12/14/16 吞吐差异 <0.4%；BS18 按显存和趋势停止 |
| C | 分离 Qwen/MMDiT mode，测试 MMDiT `reduce-overhead` / `max-autotune` | -60.18% / **+1.22%** | **完成；无 Graph 的 max-autotune 作为长训练可选项** | 五桶冷启动约 71 分钟；32 台含冷启动约 9.56 天 |
| D | 审计 grad norm，测试关闭或降低 grad-clip 频率 | **实测 -0.09%** | **完成，不进入基线** | 41 步仅首步触发；省下的可见时间被异步等待抵消，且长训有风险 |
| E | Qwen FP8 推理 | **中间 MLP +9.57%；全 MLP +13.57%；全量 +19.43%** | **完成性能/短程测试；待长程质量放行** | 三档 32 台为 8.68/8.37/7.96 天；优先验证中间 24 层 |
| F | MMDiT FP8 训练 | **单独 +21.81%；叠加后最高 +53.55%** | **完成性能/短程测试；待长程收敛放行** | 只转 80 个 token-block Linear；32 台单独 7.90 天、全 FP8 6.27 天 |

编译缓存固定放到大容量持久盘，避免五张图写满系统 `/tmp`。实测旧 `/tmp/torchinductor_root` 累计达到 13 GiB 并触发 `ENOSPC`；清理后源码、数据和 checkpoint 均未受影响。缓存迁移到 `/mnt/data/users/wfz/torchinductor-cache-uv3`。机器有 184 CPU，torch 默认每 rank 32 个编译线程会形成 256 线程过度订阅；推荐设置 `TORCHINDUCTOR_COMPILE_THREADS=8`，总计 64 个编译线程。首次构建很慢，但稳态训练与后续实验复用同一缓存；冷启动不混入 step 30→40 的吞吐。

BS14/16 都是新的 batch 静态形状，五个文本桶也都需要生成新图；在 8 rank、每 rank 8 个编译线程下，首次启动到五桶完成并进入稳态 warmup 约需 18～20 分钟。这个成本不会影响长时训练的稳态吞吐，但频繁更换 batch size 时不可忽略，因此 batch sweep 每个点都保留独立配置和缓存。

## 后续提速候选

下表同时保留已完成项的实测结果；尚未完成项仍为预估值，必须通过严格 A/B 验证：

| 优先级 | 方案 | 预计/实测端到端收益 | 质量风险 | 说明 |
|---:|---|---:|---|---|
| 1 | MMDiT 同步使用五个静态长度桶，不再补回 1024 | **实测 +17.47%** | 低 | 已完成；平均文本桶约 749 tokens，32 台估算降到 9.63 天 |
| 2 | 分桶阶段保留 token IDs，训练阶段不再重复 tokenize；形成 pinned token batch | **实测 +0.73%** | 无 | 已完成；token H2D 使用当前 stream，避免预取 stream 争用 |
| 3 | MMDiT 设置 `reshard_after_forward=False` | **实测 +0.97%** | 无 | 已完成；每卡增加约 2.32 GiB 峰值显存 |
| 4 | 静态编译 VAE encoder，并测试 `max-autotune`/CUDA Graph | **实测 +0.79%** | 低 | 已完成；固定 256×256，首次编译约 75 秒 |
| 5 | fused AdamW、减少或融合 grad-clip/optimizer 扫描 | **fused -0.27%；仅 warmup clip -0.09%** | 低到中 | 两项均无 wall-clock 收益；保持非 fused AdamW 和每步 clip |
| 6 | MMDiT `max-autotune-no-cudagraphs` | **实测 +1.22%** | 低 | 长训练可选；五桶首次搜索约 71 分钟，普通 `max-autotune` 因隐含 CUDA Graph 不采用 |
| 7 | Qwen FP8 推理 | **实测 +9.57%～+19.43%** | 中 | 三档 32 台为 8.68/8.37/7.96 天；短程 loss 正常，但真实特征 rel-L2 为 4.67%～12.14%，待长程质量放行 |
| 8 | MMDiT FP8 训练 | **单独实测 +21.81%；全 FP8 组合 +53.55%** | 高 | 80 个 token-block Linear；50 step finite，全 FP8 32 台 6.27 天，仍需长程收敛验证 |
| 9 | 文本 token resampler，将 1024 tokens 压缩到 128～256 | 20%～40% | 高 | 可显著缩短 16 个 single-stream block 的序列，但属于模型架构变化 |

多机训练应让 MMDiT 使用节点内 8 卡 shard、节点间 replicate 的 HSDP mesh，避免每层参数 all-gather 跨机器；该项主要改善多机扩展效率，不改变单机结果。

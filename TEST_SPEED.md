# 训练速度测试

## 3B 模型处理 1B 图文样本的时间估算

测试条件：

- 模型参数量：3.13B
- 每台机器：8 张 GPU
- 单卡 batch size：12
- 单机全局 batch size：96
- 输入包含真实文本，text encoder 为冻结的 Qwen3.5-9B
- 文本固定 pad 到 1024 tokens，图片 token 固定为 256
- 使用 FSDP2、Flex Attention 和静态 `torch.compile`
- MMDiT 与 text encoder 均启用 `torch.compile`

单机实测稳态结果：

> 本节初始结果使用 trainer 的 `timing` 计算段口径：batch 已经取出后开始计时，不包含下一批数据读取和 CPU 分桶/tokenize。后面的长度桶章节同时给出严格 wall-clock 口径。

| 配置 | 耗时/step | 吞吐 | 峰值显存/卡 |
|---|---:|---:|---:|
| AdamW + 关闭 self-flow | 3.16 秒 | 30.4 样本/秒 | 64.8 GiB |
| Muon + self-flow | 4.54 秒 | 21.1 样本/秒 | 66.4 GiB |

AdamW + 关闭 self-flow 相比 Muon + self-flow 综合提速约 **1.44 倍**，耗时下降约 **30.4%**。

以下按 1B 图文样本计算。多机按接近线性扩展估算，扩展效率依次采用 100%、99%、98%、97%、96%、95%；没有计入 FSDP 显存进一步下降后增大 batch 可能带来的额外利用率收益。

| 机器数 | GPU 数 | AdamW + 关闭 self-flow | Muon + self-flow |
|---:|---:|---:|---:|
| 1 | 8 | **381.1 天** | 547.3 天 |
| 2 | 16 | **192.5 天** | 276.4 天 |
| 4 | 32 | **97.2 天** | 139.6 天 |
| 8 | 64 | **49.1 天** | 70.5 天 |
| 16 | 128 | **24.8 天** | 35.6 天 |
| 32 | 256 | **12.5 天** | 18.0 天 |

理想完全线性扩展时，32 台机器分别约需 **11.9 天**和 **17.1 天**。按上述扩展假设，AdamW + 关闭 self-flow 在 32 台机器上的预计时间约为 **11.9～12.5 天**。

## Qwen 静态文本长度桶

在 AdamW、关闭 self-flow、单卡 batch size 12 的 3B 配置上，对 Qwen 使用五个静态长度桶：

- 桶长度：`512 / 640 / 768 / 896 / 1024`
- 调度权重：`5 / 4 / 4 / 3 / 4`，匹配当前数据的文本长度分布
- 每个 rank 在同一步使用相同长度桶
- 每张卡保留一份 Qwen 权重，并缓存五张静态 `torch.compile` 图
- 所有图均为 `dynamic=False`
- Qwen 输出补回 1024，MMDiT 继续使用单一 1024-token 静态图
- 前 20 步用于五张图的编译预热，使用后续两个完整 10 步窗口计时

严格 A/B 的训练计算段结果（不含下一批读取和 CPU 分桶/tokenize）：

| Qwen 输入方案 | 耗时/step | 单机吞吐（全局 BS=96） | 峰值显存/卡 |
|---|---:|---:|---:|
| 固定 1024 baseline | 3.157 秒 | 30.41 样本/秒 | 64.8 GiB |
| 四桶：640～1024 | 2.755 秒 | 34.84 样本/秒 | 64.8 GiB |
| 五桶：512～1024 | **2.729 秒** | **35.17 样本/秒** | 64.8 GiB |

五桶相比固定 1024 的训练计算段：

- 吞吐提升 **15.68%**
- 单步时间下降 **13.55%**
- 加速比 **1.157 倍**
- 五桶相比四桶额外提升约 **0.94%**

严格 wall-clock 结果使用预热后最后 10 步的累计 `spd` 差分，包含数据读取、CPU 分桶和 tokenize：

| Qwen 输入方案 | wall-clock 耗时/step | 单机端到端吞吐（全局 BS=96） |
|---|---:|---:|
| 固定 1024 baseline | 3.293 秒 | 29.15 样本/秒 |
| 五桶：512～1024 | **2.923 秒** | **32.84 样本/秒** |

五桶的真实端到端吞吐提升为 **12.65%**，wall-clock 单步时间下降 **11.23%**。计算段与 wall-clock 收益之间约 3 个百分点的差距，主要来自当前分桶路径会在 CPU 上额外做一次长度 tokenize，随后训练路径再次 tokenize。

按 1B 图文样本估算：

| 机器数 | GPU 数 | 固定 1024 | Qwen 五桶 |
|---:|---:|---:|---:|
| 1 | 8 | 397.0 天 | **352.4 天** |
| 2 | 16 | 200.5 天 | **178.0 天** |
| 4 | 32 | 101.3 天 | **89.9 天** |
| 8 | 64 | 51.2 天 | **45.4 天** |
| 16 | 128 | 25.8 天 | **22.9 天** |
| 32 | 256 | 13.1 天 | **11.6 天** |

多机时间继续采用 100%、99%、98%、97%、96%、95% 的扩展效率假设。按严格 wall-clock 口径，五桶方案在单机处理 1B 样本时预计节省约 **44.6 天**。

## Tokenize once、FSDP 不 reshard 与 VAE compile 实测

在五桶方案上继续逐项叠加优化。每项均使用相同真实图文数据、全局 batch size 96、8 张 H20；前 20 step 预热，使用 step 30 到 step 40 的累计 `spd` 差分计算严格 wall-clock。结果如下：

| 逐项配置 | wall-clock 耗时/step | 单机吞吐 | 相对上一项吞吐 | 相对五桶 baseline | 训练计算段/step | 峰值显存/卡 |
|---|---:|---:|---:|---:|---:|---:|
| 五桶 baseline | 2.923 秒 | 32.84 样本/秒 | — | — | 2.729 秒 | 64.82 GiB |
| + tokenize once、pinned token batch | 2.902 秒 | 33.08 样本/秒 | **+0.73%** | +0.73% | 2.729 秒 | 64.82 GiB |
| + `reshard_after_forward=False` | 2.874 秒 | 33.40 样本/秒 | **+0.97%** | +1.71% | 2.694 秒 | 67.14 GiB |
| + VAE `_encode` `max-autotune` compile | **2.852 秒** | **33.67 样本/秒** | **+0.79%** | **+2.51%** | 2.671 秒 | 66.87 GiB |
| + fused AdamW（不推荐） | 2.859 秒 | 33.57 样本/秒 | -0.27% | +2.24% | **2.657 秒** | 66.81 GiB |

实现与结论：

- 分桶判断长度时保存 `input_ids`，训练阶段不再二次 tokenize；按 `(双缓冲 slot, 桶长度)` 复用 pinned `input_ids/attention_mask`。token 在 Qwen 当前 CUDA stream 上有序异步 H2D。把下一批 token 放到 image prefetch stream 会产生跨 stream 争用，实测回退约 5%，因此没有采用。
- `reshard_after_forward=False` 省掉 backward 前的参数 all-gather，端到端提升 0.97%，每卡峰值显存增加约 2.32 GiB；当前 H20 显存充足，建议启用。
- VAE 只编译固定形状、纯 tensor 的 `vae._encode`，使用 `dynamic=False, mode="max-autotune"`。稳态端到端提升 0.79%；首次编译的 VAE 段约 75 秒，之后使用编译缓存。
- fused AdamW 将可见 optimizer 调用段从约 0.363 秒降至 0.181 秒，但等待转移到更早的同步点，严格 wall-clock 没有收益。保留 `adam_fused` 可选开关，但推荐保持关闭。
- 非 fused 配置不会显式传 `fused=False`，而是保留 PyTorch 的 `fused=None/foreach=None` 自动选择。显式传 `fused=False` 会禁用 torch 2.10 的 CUDA 快速默认路径，在本测试中会把 optimizer 段放大到约 1.55 秒/step。

推荐组合相对原五桶 baseline 的端到端吞吐累计提升 **2.51%**，单步时间下降 **2.45%**。按 1B 图文样本估算：

| 机器数 | GPU 数 | 五桶 baseline | 推荐组合 |
|---:|---:|---:|---:|
| 1 | 8 | 352.4 天 | **343.8 天** |
| 2 | 16 | 178.0 天 | **173.6 天** |
| 4 | 32 | 89.9 天 | **87.7 天** |
| 8 | 64 | 45.4 天 | **44.3 天** |
| 16 | 128 | 22.9 天 | **22.4 天** |
| 32 | 256 | 11.6 天 | **11.3 天** |

多机时间仍采用 100%、99%、98%、97%、96%、95% 的扩展效率假设。

## 后续提速候选

下表同时保留已完成项的实测结果；尚未完成项仍为预估值，必须通过严格 A/B 验证：

| 优先级 | 方案 | 预计/实测端到端收益 | 质量风险 | 说明 |
|---:|---|---:|---|---|
| 1 | MMDiT 同步使用五个静态长度桶，不再补回 1024 | 10%～20% | 低 | 平均文本桶约 749 tokens，文本+图片总长度可从 1280 降至约 1005；五种总长度仍都是 128 的倍数 |
| 2 | 分桶阶段保留 token IDs，训练阶段不再重复 tokenize；形成 pinned token batch | **实测 +0.73%** | 无 | 已完成；token H2D 使用当前 stream，避免预取 stream 争用 |
| 3 | MMDiT 设置 `reshard_after_forward=False` | **实测 +0.97%** | 无 | 已完成；每卡增加约 2.32 GiB 峰值显存 |
| 4 | 静态编译 VAE encoder，并测试 `max-autotune`/CUDA Graph | **实测 +0.79%** | 低 | 已完成；固定 256×256，首次编译约 75 秒 |
| 5 | fused AdamW、减少或融合 grad-clip/optimizer 扫描 | **fused 实测 -0.27%** | 低 | fused 不启用；grad-clip/扫描融合仍可继续测 |
| 6 | Qwen FP8 推理 | 3%～8% | 中 | H20 支持 FP8，环境已有 Transformer Engine 和 torchao；需验证文本特征误差 |
| 7 | MMDiT FP8 训练 | 15%～30% | 中到高 | 潜在收益大，但需要 loss/收敛和数值稳定性验证 |
| 8 | 文本 token resampler，将 1024 tokens 压缩到 128～256 | 20%～40% | 高 | 可显著缩短 16 个 single-stream block 的序列，但属于模型架构变化 |

多机训练应让 MMDiT 使用节点内 8 卡 shard、节点间 replicate 的 HSDP mesh，避免每层参数 all-gather 跨机器；该项主要改善多机扩展效率，不改变单机结果。
